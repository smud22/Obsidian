Дело в том, что обычный метод может изменить объект. Константная ссылка запрещает изменения, а значит, запрещает вызовы обычных методов. Параметр `cat` имеет тип константной ссылки (`const Cat&`), поэтому не позволяет вызывать обычные методы. Нужен способ сообщить компилятору, что метод не будет модифицировать объект.

Такой способ есть — нужно сделать метод константным. Для этого между списком параметров и телом метода нужно написать спецификатор `const`.

```cpp
class Cat {
    ...
    // Cпецификатор const означает, что метод константный.
    //                      ↓
    std::string GetName() const {
        return name_;
    }
    ...
}; 
```

После этого изменения программа скомпилируется без ошибок.

В стандартной библиотеке константные методы встречаются повсеместно. Вот лишь несколько примеров:

- Методы [`std::string::length`](https://en.cppreference.com/w/cpp/string/basic_string/size) и [`std::vector::size`](https://en.cppreference.com/w/cpp/container/vector/size) возвращают длину строки и вектора, не модифицируя их.
- Методы [`std::string::find`](https://en.cppreference.com/w/cpp/string/basic_string/find), [`std::string::find_first_of`](https://en.cppreference.com/w/cpp/string/basic_string/find_first_of), [`std::string::find_last_of`](https://en.cppreference.com/w/cpp/string/basic_string/find_last_of) ищут вхождение подстроки или символа в строке.
- Методы [`std::string::empty`](https://en.cppreference.com/w/cpp/string/basic_string/empty) и [`std::vector::empty`](https://en.cppreference.com/w/cpp/container/vector/empty) сообщают, пустые строки и векторы или нет.
- Метод [`std::string::substr`](https://en.cppreference.com/w/cpp/string/basic_string/substr) извлекает подстроку из строки, не модифицируя оригинальную строку.

Спецификатор `const` — это не просто украшение. Компилятор не разрешает внутри константного метода менять состояние объекта. Компилятор выдаст ошибку, если вы:

- попытаетесь присвоить полю класса новое значение;
- попытаетесь вызвать неконстантный метод текущего объекта;
- попытаетесь вызвать у поля класса неконстантный метод (если поле — класс или структура);
- попытаетесь передать поле класса в функцию, которая принимает аргумент по неконстантной ссылке;
- делаете всё вышеперечисленное через `this`.

```cpp
class Cat {
    ...
    std::string GetName() const {
        name_ = "Unknown name"s; // Ошибка: нельзя изменить значение поля класса 
                                 // внутри константного метода.

        name_.push_back('!');    // Ошибка: нельзя вызвать неконстантный метод 
                                 // у поля класса.

        NonConstMethod();        // Ошибка: нельзя вызвать неконстантный метод 
                                 // у текущего объекта.

        this->NonConstMethod();  // Ошибка: всё вышеперечисленное нельзя делать
                                 // через this.
        return name_;
    }
    ...
    void NonConstMethod() {
    }
}; 
```

Иными словами — не пытайтесь изменить состояние текущего объекта внутри константного метода. Внутри константного метода текущий объект считается константным, и компилятор следит, чтобы вы не пытались его изменить. Так компилятор может гарантировать неизменность объектов, изначально объявленных константными или принимаемых по константной ссылке.

💡 У константных экземпляров класса разрешается вызывать только константные методы. То же самое касается вызова метода у объектов, доступных по константным ссылкам.
![[Pasted image 20251218210101.png]]