Когда вы записываете объект в поток, состояние объекта преобразуется в последовательность символов. Затем эта последовательность может быть восстановлена из потока в виде другого объекта. Если поток связан с файлом, этот файл можно скопировать на другой компьютер и восстановить объект там.

Именно это происходит в большинстве редакторов документов. Программа записывает в выбранный файл все относящиеся к редактируемому документу данные, когда вы выбираете в меню пункт «Сохранить». Когда вы выбираете в меню пункт «Открыть», она загружает документ из файла, восстанавливая сохранённые в нём объекты.

Процесс сохранения состояния объекта в последовательность байтов или символов называется **сериализацией**. Обратный процесс — **десериализацией**.

Описанная ранее функция `WritePoint` сериализует структуру `Point` в виде двух чисел, разделённых пробелом. Например, точку с координатами (10, 35) она запишет в виде строки «10 35». Пробел между координатами позволяет понять, где заканчивается одна координата и начинается другая. Функция `ReadPoint` выполняет обратную задачу — десериализует строку «10 35» в координаты (10, 35).

💡 Функции сериализации и десериализации разрабатываются совместно и используют один и тот же формат данных. Функция десериализации должна понимать данные, которые записала функция сериализации.