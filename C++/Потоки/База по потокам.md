Вы уже знакомы с тремя стандартными потоками `std::cin`, `std::cout`, `std::cerr`, которые служат для ввода и вывода данных через консоль. В этом уроке вы узнаете об этих и других потоках подробнее.

На самом деле `std::cin`, `std::cout` и `std::cerr` — это не сами потоки ввода и вывода. Это глобальные объекты, которые предназначены для работы с тремя стандартными потоками библиотеки языка C:

- `stdin` (англ. standard input stream) — стандартный поток ввода, с ним связан объект `std::cin`;
- `stdout` (англ. standard output stream) — стандартный поток вывода, с ним связан объект `std::cout`;
- `stderr` (англ. standard error output stream) — стандартный поток вывода ошибок, с ним связан объект `std::cerr`.

Слово «глобальные» означает, что эти объекты, как и обычные глобальные переменные, инициализируются при запуске программы и существуют до её завершения. К ним можно обращаться из любого места программы.

Слово «объекты» означает, что это экземпляры некоторых классов:

- `std::cin` — это объект класса `std::istream` (англ. input stream, «поток ввода»),
- `std::cout` и `std::cerr` — это объекты класса `std::ostream` (англ. output stream, «поток вывода»).

Эти объекты объявлены в стандартной библиотеке C++. Они становятся доступны, когда вы подключаете библиотеку ввода-вывода директивой `#include <iostream>`.

Класс потока ввода предоставляет операции для чтения данных, а класс потока вывода — операции для записи. Поэтому из потока чтения можно только читать данные, а в поток вывода — только записывать.

Когда вы выводите какие-либо данные в `std::cout`, он преобразовывает их в последовательность символов и отправляет в поток `stdout`. То же самое происходит с другими потоками: ввод из `std::cin` синхронизируется с вводом из `stdin`, а вывод в `std::cerr` — с выводом в `stderr`. Такое решение позволяет в одной программе смешивать ввод-вывод на C и на C++.

```cpp
#include <iostream>
#include <stdio.h> // Подключаем стандартную библиотеку ввода-вывода языка C.

int main() {
    // Выводим строку "Hello " в стандартный 
    // поток вывода, используя объект std::cout.
    std::cout << "Hello ";

    // Выводим строку "world" в поток stdout функцией fputs библиотеки языка C.
    fputs("world", stdout);

    // Функция puts выводит текст в поток stdout.
    puts("!\n");
} 
```

Эта программа выведет строку `Hello world!`.

💡 В одной программе действительно можно смешивать ввод-вывод в объекты потоков C++ с функциями ввода-вывода языка C. Однако возможность не означает, что это хорошая практика. Код, в котором ввод-вывод выполняется в разном стиле, сложнее читать. В программах на C++ используйте средства ввода-вывода языка C++ — они удобнее и безопаснее функций ввода-вывода языка C.

Многие методы чтения и записи данных, а также операции `<<` и `>>` возвращают ссылку на поток, к которому они относятся. Благодаря этому работает чтение и запись нескольких значений:

```cpp
int main() {
    int x;
    float y;
    
    std::cin >> x >> y;
    // Этот код аналогичен следующему:
    // (std::cin >> x) >> y;
} 
```

Вот как работает операция `std::cin >> x >> y`:

1. Сначала выполняется выражение `std::cin >> x`, которое считывает целое число в переменную `x` и возвращает ссылку на этот же поток — `std::cin`.
2. Затем выполняется операция `std::cin >> y`, которая считывает число с плавающей запятой в переменную `y`.