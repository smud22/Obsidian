Ещё одно распространённое применение потоков — файловый ввод-вывод. Для этого служат следующие классы, объявленные в заголовочном файле `<fstream>`:

- `std::ifstream` (англ. input file stream) — поток для чтения из файла.
- `std::ofstream` (англ. output file stream) — поток для вывода в файл.
- `std::fstream` (англ. file stream) — поток для чтения и записи данных в файл.

Эти классы связывают объект потока с файлом на диске. Чтобы открыть файл и начать с ним работать, используется метод `open`. Он принимает два параметра:

- Имя файла.
- Режим открытия файла (опциональный параметр). Более подробно режимы открытия файла будут рассмотрены в одной из будущих тем.

Когда работа с файлом завершена, его можно закрыть методом [`close`](https://en.cppreference.com/w/cpp/io/basic_ofstream/close). Этот метод будет вызван автоматически, как только произойдёт выход из области видимости файловой переменной.

Эта программа сохраняет строку в файл filename.txt, а затем загружает её:

```cpp
#include <cassert>
#include <iostream>
#include <fstream>
#include <string>

int main() {
    using namespace std::literals;

    std::ofstream out;
    // Открываем файл filename.txt для записи и связываем его с потоком out.
    out.open("filename.txt");
    
    // Метод is_open возвращает true, если поток связан с файлом, и false — в случае ошибки.
    if (!out.is_open()) {
        std::cerr << "Failed to open file for writing" << std::endl;
        return 1;
    }
    out << "Hello!\nHow are you?\n";
    // Закрываем файл.
    out.close();

    // Открываем файл для чтения, передав его имя в конструктор.
    std::ifstream in{"filename.txt"};
    if (!in.is_open()) {
        std::cerr << "Failed to open file for reading" << std::endl;
        return 1;
    }
    std::string text;
    // Считываем данные из потока построчно в переменную text.
    for (std::string line; std::getline(in, line);) {
        text += line;
        text += '\n';
    }
    std::cout << text;
    // В std::cout будет выведено содержимое файла filename.txt.
} 
```

Файловые потоки буферизируют данные, которые в них записываются. Это значит, что поток сначала записывает данные в буфер в оперативной памяти компьютера, а после заполнения буфера записывает данные на диск. При закрытии потока содержимое буфера записывается в файл. Поэтому в этом примере у потока `out` вызывается метод `close` и лишь потом этот файл открывается для чтения.

💡 Поток закрывается автоматически при выходе из блока, где объявлена его переменная. Поэтому в большинстве случаев метод `close` вызывать не нужно. Однако иногда поток приходится закрывать заранее — тут метод и пригодится.

Функцию `main` из предыдущей программы можно переписать без вызова `close`. Для этого нужно заключить код вывода в файл filename.txt в блок.

```cpp
int main() {
    using namespace std::literals;

    {
        std::ofstream out;
        out.open("filename.txt");
    
        if (!out.is_open()) {
            std::cerr << "Failed to open file for writing" << std::endl;
            return 1;
        }
        out << "Hello!\nHow are you?\n";
    } // Поток out будет закрыт при выходе из этого блока.

    std::ifstream in{"filename.txt"};
    if (!in.is_open()) {
        std::cerr << "Failed to open file for reading" << std::endl;
        return 1;
    }
    std::string text;
    for (std::string line; std::getline(in, line);) {
        text += line;
        text += '\n';
    }
    std::cout << text;
} 
```