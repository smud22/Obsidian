💡 Жизнь любого объекта начинается с конструктора: явного или неявного. Помимо обычных конструкторов, компилятор может создавать копирующий конструктор, который вызывается при создании объекта на основе другого объекта. Например, в программе выше можно добавить строку `Alice alice3 = alice2;`. Тогда в переменной `alice3` будет объект, который ничего не вывел. Всё потому, что он был создан с помощью копирующего конструктора. Копирующий конструктор тоже можно переопределять, но об этом — далее в курсе.